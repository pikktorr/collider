{
  "name": "collider",
  "version": "1.0.0",
  "description": "",
  "main": "./src/js/index.js",
  "scripts": {
    "start": "rm -rf .cache dist && parcel src/index.html",
    "cleanup": "rm -rf .cache dist",
    "predeploy": "rm -rf .cache dist && parcel build src/index.html --public-url /collider/",
    "deploy": "gh-pages -d dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pikktorr/collider.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/pikktorr/collider/issues"
  },
  "homepage": "https://pikktorr.github.io/collider",
  "devDependencies": {
    "gh-pages": "^2.2.0",
    "parcel-bundler": "^1.12.4",
    "sass": "^1.26.7"
  }
}
